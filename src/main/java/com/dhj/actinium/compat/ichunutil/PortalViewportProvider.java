package com.dhj.actinium.compat.ichunutil;

import org.embeddedt.embeddium.impl.render.viewport.ViewportProvider;

/**
 * Marks a viewport whose projection and model-view matrices are controlled by iChun's recursive portal renderer.
 */
public interface PortalViewportProvider extends ViewportProvider {
}
